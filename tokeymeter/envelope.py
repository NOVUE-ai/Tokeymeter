"""Compatibility alias (K2) — canonical: tokeymeter.engines.optimization.envelope. Identity-aliased: this name IS the canonical module."""
import sys as _sys
from tokeymeter.engines.optimization import envelope as _canonical
_sys.modules[__name__] = _canonical
